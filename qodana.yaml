version: 1.0
linter: jetbrains/qodana-jvm:2023.2-eap
profile:
  name: qodana.recommended
exclude:
  - name: UnusedSymbol
  - name: RedundantWith
    paths:
      - exposed-core/src/main/kotlin/org/jetbrains/exposed/sql/SchemaUtils.kt
bootstrap: |
    mkdir -p /data/coverage
    mv exposed-core/build/reports/kover/report.xml /data/coverage
